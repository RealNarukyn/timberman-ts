export interface Position {
  x: number;
  y: number;
}

export enum facingENUM {
  // eslint-disable-next-line no-unused-vars
  LEFT,
  // eslint-disable-next-line no-unused-vars
  RIGHT,
  // eslint-disable-next-line no-unused-vars
  NONE
}
